package test.mypac;

public interface Remocon {
	// 필드도 정의 할 수 있지만, static final만 가능하다. 
	// static : static 영역에 Remocon Interface와 같이 생성된다.
	// final  : 값이 결정된 이후에 변경 불가능(상수화 시킨다.)
	// 생략가능
	
	// final 상수는 관례상 필드명을 모두 대문자로 작성
	public static String COMPANY="KAKAO";  
	
	
	// 메소드는 미완성의 추상 메소드만 멤버로 가질 수 있다. 
	public void up();
	public void down();
}

